/// PiCloud CLI library — exposes tracer and utilities for testing.
pub mod tracer;

/// Command helpers — URL path builders, SSE parsing, and other pure functions
/// extracted from main.rs for testability.
pub mod commands;
